"use strict";

import { scrapInsideHtmlTag } from "./src/modules/scrapInsideHtmlTag.js";

export const version = "1.0.0";

const scrapme = {
    version,
    scrapInsideHtmlTag
}

export default scrapme